import Notifications from '../types/notifications';
import { NewOrder, NewOrderParams, OrderData, OrderSearchParams } from '../types/order';
import { SetUpTypes } from '../types/setUp';
declare const _default: {
    setUp: (setUpConfig: SetUpTypes) => Promise<void>;
    createOrder: ({ amount, description, callbackFail, callbackSuccess, notificationUrl, }: NewOrderParams) => Promise<NewOrder>;
    getOrder: (orderId: string) => Promise<OrderData>;
    getFailedNotifications: () => Promise<Notifications>;
    getOrders: (orderSearchParameters?: OrderSearchParams | undefined) => Promise<OrderData[]>;
};
export = _default;
