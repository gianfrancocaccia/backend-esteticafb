import { ErrorCodes, ErrorTypes } from '../types/errors';
declare class UalaError extends Error {
    readonly message: string;
    readonly type: ErrorTypes;
    readonly statusCode: number | undefined;
    constructor(erroMessage?: string, errorCode?: ErrorCodes, statusCode?: number);
}
export default UalaError;
